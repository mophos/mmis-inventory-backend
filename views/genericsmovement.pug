include css.pug
doctype html
html
  head
    meta(charset="utf-8")
    title report
  body
    button.btn.btn-sm.btn-success(onclick='window.print()') Print
    table.table
      thead
        tr 
          td.center.bold(colspan='7') #{hospitalName}
        tr 
          td.center.bold(colspan='7') รายการคงคลังเฉพาะที่มีการเคลื่อนไหว
        tr 
          td.left(colspan='7') #{printDate}
        tr
          th.left #
          th.left รหัสยา
          th.left ชื่อยา
          th.right จำนวน &nbsp;
          th.left หน่วย
          th.left lot
          th.left ชนิด
      tbody
      - y=0
      for i in generics
        - y++
        tr
          td.left #{y} &nbsp;
          td.left #{i.product_code}
          td.left #{i.product_name}
          td.right #{i.qty} &nbsp;
          td.left #{i.package}
          td.left #{i.lot_no}
          td.left #{i.generic_type_name}

