include css.pug
doctype html
html
  head
    meta(charset="utf-8")
    title report
  body
    button.btn.btn-sm.btn-success(onclick='window.print()') Print
    table.table
      thead
        tr 
          td.center.bold(colspan='4') #{hospitalName}
        tr 
          td.center.bold(colspan='4') รายการเฉพาะที่ไม่มีการเคลื่อนไหว
        tr 
          td.left(colspan='4') #{printDate}
        tr
          th.left #
          th.left รหัสยา
          th.left ชื่อยา
          th.left ชนิด
      tbody
      - y=0
      for i in generics
        - y++
        tr
          td.left #{y}
          td.left #{i.working_code}
          td.left #{i.generic_name}
          td.left #{i.generic_type_name}

