include css.pug
style(type='text/css').
  body{
  font-family:'THSarabunNewIT';
  }
style(type='text/css' media='print').
   @page{
   margin-top: 1.5cm;
   margin-left: 1.5cm;
   margin-right: 2cm;
   }
doctype html
html
  head
    meta(charset="utf-8")
    title report
  body
    button.btn.btn-sm.btn-success.btnprint(onclick='window.print()') Print
    .center.bold บันทึกการตรวจรับพัสดุ
    br
    table.table
      tr
        td.left เลขที่รับ #{check_receive[0].receive_code}
        td.right เขียนที่ #{hospitalName}
    div(style='margin-left:50%;') วันที่ #{today}
    br
    .fontspace
    | ตามบันทึกที่ #{check_receive[0].purchase_order_book_number} ลงวันที่ #{check_receive[0].receive_date} ได้แต่งตั้งคณะกรรมการตรวจรับพัสดุนั้น บัดนี้คณะกรรมการตรวจรับพัสดุได้ร่วมกันตรวจพัสดุซึ่ง 
    | #{check_receive[0].labeler_name} ผู้ขายได้นำส่งมอบเมื่อ วันที่ #{check_receive[0].delivery_date} ตามใบส่งของเลขที่ #{check_receive[0].delivery_code} จำนวน #{qty} รายการ 
    |	รวมเป็นเงินสุทธิ #{check_receive[0].total_price} บาท (#{bahtText}) 
    br
    .fontspace
    | คณะกรรมการตรวจรับพัสดุเห็นว่ามีปริมาณและคุณภาพถูกต้องครบถ้วน และได้ส่งมอบให้เจ้าหน้าที่พัสดุรับไว้เรียบร้อยแล้ว 
    br
    .fontspace
    | จึงพร้อมกันลงลายมือชื่อไว้เป็นหลักฐาน
    br
    br
    table(style='width:55%;margin-left:100px;')
      tr
        td.right(width="5%") (ลงชื่อ)
        td.center(width="50%") ...........................................................
        td.left ประธานกรรมการตรวจรับ
      tr
        td
        td.center (#{committee[0].title_name}#{committee[0].fname} #{committee[0].lname})
        td
      tr
        td.center(height='20px')
        td
        td
      tr
        td.right (ลงชื่อ)
        td.center ...........................................................
        td.left กรรมการตรวจรับ
      tr
        td
        td.center (#{committee[1].title_name}#{committee[1].fname} #{committee[1].lname})
        td
      tr
        td.center(height='20px')
        td
        td
      tr
        td.right (ลงชื่อ)
        td.center ...........................................................
        td.left กรรมการตรวจรับ
      tr
        td
        td.center (#{committee[2].title_name}#{committee[2].fname} #{committee[2].lname})
        td
      tr
        td.center(height='20px')
        td
        td
    | ได้รับมอบพัสดุรายการข้างต้นจากคณะกรรมการตรวจรับพัสดุไว้แล้วเพื่อเบิกจ่ายต่อไป
    br
    table(style='width:55%;margin-left:100px;')
      tr
        td.right(width="5%") (ลงชื่อ)
        td.center(width="50%") ...........................................................
        td.left เจ้าหน้าที่
      tr
        td
        if(invenChief[0].buyer_fullname=='')
          td.center 
        if(invenChief[0].buyer_fullname!='') 
          td.center (#{invenChief[0].buyer_fullname})
        td
    //- br
    //- | (ลงชื่อ).....................................................เจ้าหน้าที่พัสดุ 
    //- br
    //- div(style='width: 20%; margin-left:10%;')
    //-  | (#{invenChief[0].buyer_fullname})
    br
    | เรียน  ผู้ว่าราชการจังหวัด#{province}
    br
    .fontspace
    | คณะกรรมการได้ตรวจรับเวชภัณฑ์ ดังกล่าวถูกต้องแล้ว การส่งมอบ ( &nbsp; ) ภายในกำหนด ( &nbsp; ) เกินกำหนด เห็นควรอนุมัติให้เบิกจ่ายเงินบำรุงโรงพยาบาลประจำปีงบประมาน 2561 แก่ผู้ขายต่อไป
    br
    br
    br
    table(style='width:55%;margin-left:100px;')
      tr
        td.right(width="5%") (ลงชื่อ)
        td.center(width="50%") ...........................................................
        td.left 
      tr
        td
        td.center (#{invenChief[0].chief_fullname})
        td
    br
    br
    div(style='width:50px;margin-left:100px;') ทราบ
    br
    br
    table(style='width:55%;margin-left:100px;')
      tr
        td.right(width="5%") (ลงชื่อ)
        td.center(width="50%") ...........................................................
        td.left 
      tr
        td.center 
        td.center  
          | (#{master})
          br
          | ผู้อำนวยการ#{hospitalName}
        td
