include css.pug
doctype html
html
  head
    meta(charset="utf-8")
    title report
  body
    button.btn.btn-sm.btn-success(onclick='window.print()') Print
    table.table
      thead
        tr 
          td.center.bold(colspan='9') #{hospitalName} 
        tr 
          td.center.bold(colspan='9') รายงานสรุปการเบิก
        tr 
          td.center(colspan='9') ประจำวันที่ #{requis[0].check_date}
        tr 
          td.left(colspan='9') ใบเบิกเลขที่ #{requis[0].requisition_id}
        tr 
          td.left(colspan='9') ณ วันที่ #{today}
      tbody
      tr
        td รหัสยา
        td ชื่อเวชภัณฑ์ 
        td
        td จำนวน
        td ราคาต่อหน่วย
        td ราคารวม
      for a in requis
        tr.dash-top
          td #{a.product_id} 
          td #{a.product_name}
          td #{a.generic_name}
          td #{a.qty}
          td #{a.cost_unit}
          td #{a.cost}
        